# 学生信息管理系统升级报告 (v2.0)

针对系统的进一步优化与深入需求，本次 v2.0 升级进行了全方位的架构重构和功能扩展，使得项目脱离了简单的 MVP，成为一个更为成熟的企业级中后台系统模板。

## 1. 架构深度解耦与拓展

### 1.1 后端路由重构
原本所有的接口都集中在 `main.py` 中，这在大型项目中会导致代码难以维护。
- 引入了 `APIRouter`，将路由按照业务域拆分到 `routers/` 目录下，包含：
  - `auth_router.py`: 认证与用户鉴权。
  - `students_router.py`: 学生实体 CRUD 及文件导出。
  - `courses_router.py`: 课程实体管理。
  - `system_router.py`: Dashboard 看板与系统操作审计。
- 主程序 `main.py` 变得极为清爽，仅负责应用的初始化、中间件挂载与路由组装。

### 1.2 数据模型扩展 (多表关联)
数据库设计突破了单表限制：
- 增加了 `courses` (课程表) 记录课程基础信息。
- 增加了 `enrollments` (选课关联表) 记录学生、课程、学期与最终成绩。
- 增加了 `audit_logs` (审计日志表) 实现对核心系统操作的防篡改追踪。
并使用 `Alembic` 进行了平滑的结构升迁 (Migration)。

## 2. 功能全面深化

### 2.1 批量操作与数据导出
- 在前端学生管理列表中，加入了**多选框 (Checkbox)** 和 **批量删除**功能。
- 增加了 **CSV 文件导出**功能，使用原生 HTML / FastAPI Response 流将学生列表导出为供 Excel 解析的结构化表格，提升了管理人员的工作效率。

### 2.2 审计日志 (Audit Logging)
- 封装了通用的 `log_audit` 动作追踪方法，拦截所有的增(CREATE)、删(DELETE)、改(UPDATE)、导出(EXPORT)操作，并统一入库。
- 前端新增了“审计日志”视图，帮助超级管理员实时追踪系统数据状态流转，提升了系统的安全性和可追溯性。

### 2.3 复杂视图与抽屉效果
- 在学生列表页引入了 `el-drawer` (抽屉) 组件，摒弃了所有信息堆叠在表格的冗杂感，点击学生姓名侧边会滑出精美的档案详情页。
- 增加了独立的 `Courses` (课程管理) 页面。

## 3. 交互与用户体验优化 (UX)

### 3.1 增强版 Axios 封装
- 重写了前端的 API 请求封装逻辑，增加了面向不稳定网络环境的**自动重试 (Retry) 机制**（对于 5xx 服务端错误自动执行指数退避重试）。

### 3.2 黑暗模式 (Dark Mode) 支持
- 引入 `Pinia` 维护的全新 `useThemeStore`，基于 Element Plus 的 CSS 变量体系，在顶部导航栏增加一键切换“亮色/暗色”主题功能。并且记忆用户的偏好设定持久化至 `localStorage`。

### 3.3 充实的数据 Mock 库
- 重构了 `seed.py` 脚本，如今的初始化数据库不仅有学生，更会生成多重网状的关联信息（比如某位同学选择了高数和物理并附带成绩，并且系统留下了创建这些数据时的日志），保证了一键启动项目后的惊艳感和数据充实度。
